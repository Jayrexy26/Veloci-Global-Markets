name: Coinluxora Status Check

on:
  push:
    branches:
      - main

jobs:
  status-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Basic sanity check
        run: |
          echo "Coinluxora workflow running..."
          ls -la
          echo "All good ✔️"
          
